name: bazar_books_mobile_app
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  #asset gen
  flutter_gen: ^5.6.0
  #state managment
  bloc: ^8.1.4
  flutter_bloc: ^8.1.6
  #auto route
  auto_route: ^8.2.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner:
  flutter_gen_runner:
  auto_route_generator:

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/